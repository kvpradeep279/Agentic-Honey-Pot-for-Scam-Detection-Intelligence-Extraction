# ===========================================
# Agentic Honey-Pot - Dependencies
# ===========================================

# Web Framework
fastapi==0.109.0          # Modern, fast web framework for building APIs
uvicorn[standard]==0.27.0 # ASGI server to run FastAPI

# Data Validation
pydantic==2.5.3           # Data validation using Python type hints
pydantic-settings==2.1.0  # Settings management with Pydantic

# HTTP Client (for callbacks to GUVI)
httpx==0.26.0             # Async HTTP client
requests==2.31.0          # Simple HTTP client for callbacks

# AI/LLM Integration (choose one based on what you have access to)
google-generativeai==0.8.4  # Google Gemini API (free tier available!)
# openai==1.12.0            # OpenAI API (if you have credits)

# Utilities
python-dotenv==1.0.0      # Load environment variables from .env file
python-dateutil==2.8.2    # Date parsing utilities
